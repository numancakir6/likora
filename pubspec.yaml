name: likora
description: Likora App

publish_to: 'none'

version: 1.0.2+93

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_launcher_icons: ^0.14.4
  flutter_svg: ^2.0.10
  shared_preferences: ^2.5.3

  firebase_core: ^3.13.0
  firebase_auth: ^5.5.2
  cloud_firestore: ^5.6.6
  audioplayers: ^6.0.0
  google_mobile_ads: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/likora/
    - assets/lexion/
    - assets/sfx/

flutter_launcher_icons:
  ios: true
  image_path: "assets/likora/likora_ikon.png"